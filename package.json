{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "cd backend && node server.js",
    "start": "cd backend && node server.js",
    "build": "cd backend && npm install && cd .. && npm install --prefix frontend && npm run build --prefix frontend"
  }
}